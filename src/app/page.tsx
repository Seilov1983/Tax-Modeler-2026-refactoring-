import { ClientApp } from './ClientApp';

/**
 * Server Component entry point.
 * In production, project data would be fetched server-side here.
 */
export default function HomePage() {
  return <ClientApp />;
}
